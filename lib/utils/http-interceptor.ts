// Re-export the authenticated fetch utilities
export { 
  setGlobalLogout, 
  authenticatedFetch, 
  makeAuthenticatedRequest,
  isAuthenticationError,
  handleAuthenticationError
} from './authenticated-fetch'
